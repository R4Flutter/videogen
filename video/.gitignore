node_modules
dist
.DS_Store
.env

# Ignore the output video from Git but not videos you import into src/.
out

# Stock clips are re-downloadable from the script's own keywords.
public/footage

# `remotion bundle` output — a build artefact, same as out/.
build
